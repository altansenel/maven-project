/*
 * Very basic JavaBean cache
 * Created on 24 Þub 2016 ( Date ISO 2016-02-24 - Time 15:13:28 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package org.demo.cache;

import java.util.Hashtable;
import java.util.Map;

import org.demo.bean.Safe ;

/**
 * Very basic cache for Safe instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class SafeCache
{
	private final static Map<String,Safe> cache = new Hashtable<String,Safe>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param id 
	 * @return the key
	 */
	private final static String getKey( Integer id ) {
		return ""  + id  ;
	}

	/**
	 * Put the given Safe instance in the cache
	 * @param Safe instance to be stored
	 */
	public final static void putSafe(Safe safe ) {
		String key = getKey( safe.getId() ) ;
		cache.put(key, safe );
	}
	
	/**
	 * Get the Safe instance for the given primary key
     * @param id 
	 * @return the Safe instance (or null if none)
	 */
	public final static Safe getSafe( Integer id ) {
		String key = getKey( id ) ;
		return cache.get(key);
	}

	/**
	 * Removes the Safe associated with the given primary key
     * @param id 
	 */
	public final static void removeSafe ( Integer id ) {
		String key = getKey( id ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given Safe from the cache using its primary key
	 * @param Safe instance to be removed
	 */
	public final static void removeSafe (Safe safe ) { 
		String key = getKey( safe.getId() ) ;
		cache.remove(key);
	}

}
