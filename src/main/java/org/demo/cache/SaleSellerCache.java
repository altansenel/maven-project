/*
 * Very basic JavaBean cache
 * Created on 24 Þub 2016 ( Date ISO 2016-02-24 - Time 15:13:28 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package org.demo.cache;

import java.util.Hashtable;
import java.util.Map;

import org.demo.bean.SaleSeller ;

/**
 * Very basic cache for SaleSeller instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class SaleSellerCache
{
	private final static Map<String,SaleSeller> cache = new Hashtable<String,SaleSeller>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param id 
	 * @return the key
	 */
	private final static String getKey( Integer id ) {
		return ""  + id  ;
	}

	/**
	 * Put the given SaleSeller instance in the cache
	 * @param SaleSeller instance to be stored
	 */
	public final static void putSaleSeller(SaleSeller saleSeller ) {
		String key = getKey( saleSeller.getId() ) ;
		cache.put(key, saleSeller );
	}
	
	/**
	 * Get the SaleSeller instance for the given primary key
     * @param id 
	 * @return the SaleSeller instance (or null if none)
	 */
	public final static SaleSeller getSaleSeller( Integer id ) {
		String key = getKey( id ) ;
		return cache.get(key);
	}

	/**
	 * Removes the SaleSeller associated with the given primary key
     * @param id 
	 */
	public final static void removeSaleSeller ( Integer id ) {
		String key = getKey( id ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given SaleSeller from the cache using its primary key
	 * @param SaleSeller instance to be removed
	 */
	public final static void removeSaleSeller (SaleSeller saleSeller ) { 
		String key = getKey( saleSeller.getId() ) ;
		cache.remove(key);
	}

}
