/*
 * Very basic JavaBean cache
 * Created on 24 Þub 2016 ( Date ISO 2016-02-24 - Time 15:13:30 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package org.demo.cache;

import java.util.Hashtable;
import java.util.Map;

import org.demo.bean.WaybillTrans ;

/**
 * Very basic cache for WaybillTrans instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class WaybillTransCache
{
	private final static Map<String,WaybillTrans> cache = new Hashtable<String,WaybillTrans>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param id 
	 * @return the key
	 */
	private final static String getKey( Integer id ) {
		return ""  + id  ;
	}

	/**
	 * Put the given WaybillTrans instance in the cache
	 * @param WaybillTrans instance to be stored
	 */
	public final static void putWaybillTrans(WaybillTrans waybillTrans ) {
		String key = getKey( waybillTrans.getId() ) ;
		cache.put(key, waybillTrans );
	}
	
	/**
	 * Get the WaybillTrans instance for the given primary key
     * @param id 
	 * @return the WaybillTrans instance (or null if none)
	 */
	public final static WaybillTrans getWaybillTrans( Integer id ) {
		String key = getKey( id ) ;
		return cache.get(key);
	}

	/**
	 * Removes the WaybillTrans associated with the given primary key
     * @param id 
	 */
	public final static void removeWaybillTrans ( Integer id ) {
		String key = getKey( id ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given WaybillTrans from the cache using its primary key
	 * @param WaybillTrans instance to be removed
	 */
	public final static void removeWaybillTrans (WaybillTrans waybillTrans ) { 
		String key = getKey( waybillTrans.getId() ) ;
		cache.remove(key);
	}

}
