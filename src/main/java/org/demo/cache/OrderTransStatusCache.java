/*
 * Very basic JavaBean cache
 * Created on 24 Þub 2016 ( Date ISO 2016-02-24 - Time 15:13:27 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package org.demo.cache;

import java.util.Hashtable;
import java.util.Map;

import org.demo.bean.OrderTransStatus ;

/**
 * Very basic cache for OrderTransStatus instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class OrderTransStatusCache
{
	private final static Map<String,OrderTransStatus> cache = new Hashtable<String,OrderTransStatus>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param id 
	 * @return the key
	 */
	private final static String getKey( Integer id ) {
		return ""  + id  ;
	}

	/**
	 * Put the given OrderTransStatus instance in the cache
	 * @param OrderTransStatus instance to be stored
	 */
	public final static void putOrderTransStatus(OrderTransStatus orderTransStatus ) {
		String key = getKey( orderTransStatus.getId() ) ;
		cache.put(key, orderTransStatus );
	}
	
	/**
	 * Get the OrderTransStatus instance for the given primary key
     * @param id 
	 * @return the OrderTransStatus instance (or null if none)
	 */
	public final static OrderTransStatus getOrderTransStatus( Integer id ) {
		String key = getKey( id ) ;
		return cache.get(key);
	}

	/**
	 * Removes the OrderTransStatus associated with the given primary key
     * @param id 
	 */
	public final static void removeOrderTransStatus ( Integer id ) {
		String key = getKey( id ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given OrderTransStatus from the cache using its primary key
	 * @param OrderTransStatus instance to be removed
	 */
	public final static void removeOrderTransStatus (OrderTransStatus orderTransStatus ) { 
		String key = getKey( orderTransStatus.getId() ) ;
		cache.remove(key);
	}

}
