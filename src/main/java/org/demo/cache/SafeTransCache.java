/*
 * Very basic JavaBean cache
 * Created on 24 Þub 2016 ( Date ISO 2016-02-24 - Time 15:13:28 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package org.demo.cache;

import java.util.Hashtable;
import java.util.Map;

import org.demo.bean.SafeTrans ;

/**
 * Very basic cache for SafeTrans instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class SafeTransCache
{
	private final static Map<String,SafeTrans> cache = new Hashtable<String,SafeTrans>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param id 
	 * @return the key
	 */
	private final static String getKey( Integer id ) {
		return ""  + id  ;
	}

	/**
	 * Put the given SafeTrans instance in the cache
	 * @param SafeTrans instance to be stored
	 */
	public final static void putSafeTrans(SafeTrans safeTrans ) {
		String key = getKey( safeTrans.getId() ) ;
		cache.put(key, safeTrans );
	}
	
	/**
	 * Get the SafeTrans instance for the given primary key
     * @param id 
	 * @return the SafeTrans instance (or null if none)
	 */
	public final static SafeTrans getSafeTrans( Integer id ) {
		String key = getKey( id ) ;
		return cache.get(key);
	}

	/**
	 * Removes the SafeTrans associated with the given primary key
     * @param id 
	 */
	public final static void removeSafeTrans ( Integer id ) {
		String key = getKey( id ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given SafeTrans from the cache using its primary key
	 * @param SafeTrans instance to be removed
	 */
	public final static void removeSafeTrans (SafeTrans safeTrans ) { 
		String key = getKey( safeTrans.getId() ) ;
		cache.remove(key);
	}

}
