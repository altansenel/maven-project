/*
 * Very basic JavaBean cache
 * Created on 24 Þub 2016 ( Date ISO 2016-02-24 - Time 15:13:26 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */

package org.demo.cache;

import java.util.Hashtable;
import java.util.Map;

import org.demo.bean.GlobalProfile ;

/**
 * Very basic cache for GlobalProfile instances (just for the Telosys Tools demo)
 * 
 * @author Telosys Tools Generator
 *
 */
public class GlobalProfileCache
{
	private final static Map<String,GlobalProfile> cache = new Hashtable<String,GlobalProfile>() ;
	
	/**
	 * Build the cache key from the Primary Key field(s)
     * @param id 
	 * @return the key
	 */
	private final static String getKey( Integer id ) {
		return ""  + id  ;
	}

	/**
	 * Put the given GlobalProfile instance in the cache
	 * @param GlobalProfile instance to be stored
	 */
	public final static void putGlobalProfile(GlobalProfile globalProfile ) {
		String key = getKey( globalProfile.getId() ) ;
		cache.put(key, globalProfile );
	}
	
	/**
	 * Get the GlobalProfile instance for the given primary key
     * @param id 
	 * @return the GlobalProfile instance (or null if none)
	 */
	public final static GlobalProfile getGlobalProfile( Integer id ) {
		String key = getKey( id ) ;
		return cache.get(key);
	}

	/**
	 * Removes the GlobalProfile associated with the given primary key
     * @param id 
	 */
	public final static void removeGlobalProfile ( Integer id ) {
		String key = getKey( id ) ;
		cache.remove(key);
	}
	
	/**
	 * Removes the given GlobalProfile from the cache using its primary key
	 * @param GlobalProfile instance to be removed
	 */
	public final static void removeGlobalProfile (GlobalProfile globalProfile ) { 
		String key = getKey( globalProfile.getId() ) ;
		cache.remove(key);
	}

}
