/*
 * Created on 24 Þub 2016 ( Time 15:56:47 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.demo.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import org.demo.web.common.AbstractController;
import org.demo.web.common.FormMode;
import org.demo.web.common.Message;
import org.demo.web.common.MessageType;

//--- Entities
import org.demo.bean.AdminUserGivenRole;
import org.demo.bean.AdminUserGroup;
import org.demo.bean.AdminUserRole;
import org.demo.bean.AdminWorkspace;

//--- Services 
import org.demo.business.service.AdminUserGivenRoleService;
import org.demo.business.service.AdminUserGroupService;
import org.demo.business.service.AdminUserRoleService;
import org.demo.business.service.AdminWorkspaceService;

//--- List Items 
import org.demo.web.listitem.AdminUserGroupListItem;
import org.demo.web.listitem.AdminUserRoleListItem;
import org.demo.web.listitem.AdminWorkspaceListItem;

/**
 * Spring MVC controller for 'AdminUserGivenRole' management.
 */
@Controller
@RequestMapping("/adminUserGivenRole")
public class AdminUserGivenRoleController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "adminUserGivenRole";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "adminUserGivenRole/form";
	private static final String JSP_LIST   = "adminUserGivenRole/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/adminUserGivenRole/create";
	private static final String SAVE_ACTION_UPDATE   = "/adminUserGivenRole/update";

	//--- Main entity service
	@Resource
    private AdminUserGivenRoleService adminUserGivenRoleService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private AdminUserGroupService adminUserGroupService; // Injected by Spring
	@Resource
    private AdminUserRoleService adminUserRoleService; // Injected by Spring
	@Resource
    private AdminWorkspaceService adminWorkspaceService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public AdminUserGivenRoleController() {
		super(AdminUserGivenRoleController.class, MAIN_ENTITY_NAME );
		log("AdminUserGivenRoleController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "AdminUserGroup"
	 * @param model
	 */
	private void populateListOfAdminUserGroupItems(Model model) {
		List<AdminUserGroup> list = adminUserGroupService.findAll();
		List<AdminUserGroupListItem> items = new LinkedList<AdminUserGroupListItem>();
		for ( AdminUserGroup adminUserGroup : list ) {
			items.add(new AdminUserGroupListItem( adminUserGroup ) );
		}
		model.addAttribute("listOfAdminUserGroupItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "AdminUserRole"
	 * @param model
	 */
	private void populateListOfAdminUserRoleItems(Model model) {
		List<AdminUserRole> list = adminUserRoleService.findAll();
		List<AdminUserRoleListItem> items = new LinkedList<AdminUserRoleListItem>();
		for ( AdminUserRole adminUserRole : list ) {
			items.add(new AdminUserRoleListItem( adminUserRole ) );
		}
		model.addAttribute("listOfAdminUserRoleItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "AdminWorkspace"
	 * @param model
	 */
	private void populateListOfAdminWorkspaceItems(Model model) {
		List<AdminWorkspace> list = adminWorkspaceService.findAll();
		List<AdminWorkspaceListItem> items = new LinkedList<AdminWorkspaceListItem>();
		for ( AdminWorkspace adminWorkspace : list ) {
			items.add(new AdminWorkspaceListItem( adminWorkspace ) );
		}
		model.addAttribute("listOfAdminWorkspaceItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param adminUserGivenRole
	 */
	private void populateModel(Model model, AdminUserGivenRole adminUserGivenRole, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, adminUserGivenRole);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfAdminUserGroupItems(model);
			populateListOfAdminUserRoleItems(model);
			populateListOfAdminWorkspaceItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfAdminUserGroupItems(model);
			populateListOfAdminWorkspaceItems(model);
			populateListOfAdminUserRoleItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of AdminUserGivenRole found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<AdminUserGivenRole> list = adminUserGivenRoleService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new AdminUserGivenRole
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		AdminUserGivenRole adminUserGivenRole = new AdminUserGivenRole();	
		populateModel( model, adminUserGivenRole, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing AdminUserGivenRole
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		AdminUserGivenRole adminUserGivenRole = adminUserGivenRoleService.findById(id);
		populateModel( model, adminUserGivenRole, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param adminUserGivenRole  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid AdminUserGivenRole adminUserGivenRole, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				AdminUserGivenRole adminUserGivenRoleCreated = adminUserGivenRoleService.create(adminUserGivenRole); 
				model.addAttribute(MAIN_ENTITY_NAME, adminUserGivenRoleCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, adminUserGivenRole.getId() );
			} else {
				populateModel( model, adminUserGivenRole, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "adminUserGivenRole.error.create", e);
			populateModel( model, adminUserGivenRole, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param adminUserGivenRole  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid AdminUserGivenRole adminUserGivenRole, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				AdminUserGivenRole adminUserGivenRoleSaved = adminUserGivenRoleService.update(adminUserGivenRole);
				model.addAttribute(MAIN_ENTITY_NAME, adminUserGivenRoleSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, adminUserGivenRole.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, adminUserGivenRole, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "adminUserGivenRole.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, adminUserGivenRole, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			adminUserGivenRoleService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "adminUserGivenRole.error.delete", e);
		}
		return redirectToList();
	}

}
