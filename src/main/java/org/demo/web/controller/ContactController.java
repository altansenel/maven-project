/*
 * Created on 24 Þub 2016 ( Time 15:56:48 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.demo.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import org.demo.web.common.AbstractController;
import org.demo.web.common.FormMode;
import org.demo.web.common.Message;
import org.demo.web.common.MessageType;

//--- Entities
import org.demo.bean.Contact;
import org.demo.bean.ContactCategory;
import org.demo.bean.ContactExtraFields;
import org.demo.bean.SaleSeller;
import org.demo.bean.StockPriceList;

//--- Services 
import org.demo.business.service.ContactService;
import org.demo.business.service.ContactCategoryService;
import org.demo.business.service.ContactExtraFieldsService;
import org.demo.business.service.SaleSellerService;
import org.demo.business.service.StockPriceListService;

//--- List Items 
import org.demo.web.listitem.ContactCategoryListItem;
import org.demo.web.listitem.ContactExtraFieldsListItem;
import org.demo.web.listitem.SaleSellerListItem;
import org.demo.web.listitem.StockPriceListListItem;

/**
 * Spring MVC controller for 'Contact' management.
 */
@Controller
@RequestMapping("/contact")
public class ContactController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "contact";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "contact/form";
	private static final String JSP_LIST   = "contact/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/contact/create";
	private static final String SAVE_ACTION_UPDATE   = "/contact/update";

	//--- Main entity service
	@Resource
    private ContactService contactService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private ContactCategoryService contactCategoryService; // Injected by Spring
	@Resource
    private ContactExtraFieldsService contactExtraFieldsService; // Injected by Spring
	@Resource
    private SaleSellerService saleSellerService; // Injected by Spring
	@Resource
    private StockPriceListService stockPriceListService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public ContactController() {
		super(ContactController.class, MAIN_ENTITY_NAME );
		log("ContactController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "ContactCategory"
	 * @param model
	 */
	private void populateListOfContactCategoryItems(Model model) {
		List<ContactCategory> list = contactCategoryService.findAll();
		List<ContactCategoryListItem> items = new LinkedList<ContactCategoryListItem>();
		for ( ContactCategory contactCategory : list ) {
			items.add(new ContactCategoryListItem( contactCategory ) );
		}
		model.addAttribute("listOfContactCategoryItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "ContactExtraFields"
	 * @param model
	 */
	private void populateListOfContactExtraFieldsItems(Model model) {
		List<ContactExtraFields> list = contactExtraFieldsService.findAll();
		List<ContactExtraFieldsListItem> items = new LinkedList<ContactExtraFieldsListItem>();
		for ( ContactExtraFields contactExtraFields : list ) {
			items.add(new ContactExtraFieldsListItem( contactExtraFields ) );
		}
		model.addAttribute("listOfContactExtraFieldsItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "SaleSeller"
	 * @param model
	 */
	private void populateListOfSaleSellerItems(Model model) {
		List<SaleSeller> list = saleSellerService.findAll();
		List<SaleSellerListItem> items = new LinkedList<SaleSellerListItem>();
		for ( SaleSeller saleSeller : list ) {
			items.add(new SaleSellerListItem( saleSeller ) );
		}
		model.addAttribute("listOfSaleSellerItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "StockPriceList"
	 * @param model
	 */
	private void populateListOfStockPriceListItems(Model model) {
		List<StockPriceList> list = stockPriceListService.findAll();
		List<StockPriceListListItem> items = new LinkedList<StockPriceListListItem>();
		for ( StockPriceList stockPriceList : list ) {
			items.add(new StockPriceListListItem( stockPriceList ) );
		}
		model.addAttribute("listOfStockPriceListItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param contact
	 */
	private void populateModel(Model model, Contact contact, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, contact);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfContactCategoryItems(model);
			populateListOfContactExtraFieldsItems(model);
			populateListOfSaleSellerItems(model);
			populateListOfStockPriceListItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfSaleSellerItems(model);
			populateListOfContactCategoryItems(model);
			populateListOfStockPriceListItems(model);
			populateListOfContactExtraFieldsItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of Contact found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<Contact> list = contactService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new Contact
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		Contact contact = new Contact();	
		populateModel( model, contact, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing Contact
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		Contact contact = contactService.findById(id);
		populateModel( model, contact, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param contact  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid Contact contact, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				Contact contactCreated = contactService.create(contact); 
				model.addAttribute(MAIN_ENTITY_NAME, contactCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, contact.getId() );
			} else {
				populateModel( model, contact, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "contact.error.create", e);
			populateModel( model, contact, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param contact  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid Contact contact, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				Contact contactSaved = contactService.update(contact);
				model.addAttribute(MAIN_ENTITY_NAME, contactSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, contact.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, contact, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "contact.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, contact, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			contactService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "contact.error.delete", e);
		}
		return redirectToList();
	}

}
