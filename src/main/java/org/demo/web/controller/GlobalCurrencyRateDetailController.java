/*
 * Created on 24 Þub 2016 ( Time 15:56:48 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.demo.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import org.demo.web.common.AbstractController;
import org.demo.web.common.FormMode;
import org.demo.web.common.Message;
import org.demo.web.common.MessageType;

//--- Entities
import org.demo.bean.GlobalCurrencyRateDetail;
import org.demo.bean.GlobalCurrencyRate;

//--- Services 
import org.demo.business.service.GlobalCurrencyRateDetailService;
import org.demo.business.service.GlobalCurrencyRateService;

//--- List Items 
import org.demo.web.listitem.GlobalCurrencyRateListItem;

/**
 * Spring MVC controller for 'GlobalCurrencyRateDetail' management.
 */
@Controller
@RequestMapping("/globalCurrencyRateDetail")
public class GlobalCurrencyRateDetailController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "globalCurrencyRateDetail";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "globalCurrencyRateDetail/form";
	private static final String JSP_LIST   = "globalCurrencyRateDetail/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/globalCurrencyRateDetail/create";
	private static final String SAVE_ACTION_UPDATE   = "/globalCurrencyRateDetail/update";

	//--- Main entity service
	@Resource
    private GlobalCurrencyRateDetailService globalCurrencyRateDetailService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private GlobalCurrencyRateService globalCurrencyRateService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public GlobalCurrencyRateDetailController() {
		super(GlobalCurrencyRateDetailController.class, MAIN_ENTITY_NAME );
		log("GlobalCurrencyRateDetailController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "GlobalCurrencyRate"
	 * @param model
	 */
	private void populateListOfGlobalCurrencyRateItems(Model model) {
		List<GlobalCurrencyRate> list = globalCurrencyRateService.findAll();
		List<GlobalCurrencyRateListItem> items = new LinkedList<GlobalCurrencyRateListItem>();
		for ( GlobalCurrencyRate globalCurrencyRate : list ) {
			items.add(new GlobalCurrencyRateListItem( globalCurrencyRate ) );
		}
		model.addAttribute("listOfGlobalCurrencyRateItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param globalCurrencyRateDetail
	 */
	private void populateModel(Model model, GlobalCurrencyRateDetail globalCurrencyRateDetail, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, globalCurrencyRateDetail);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfGlobalCurrencyRateItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfGlobalCurrencyRateItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of GlobalCurrencyRateDetail found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<GlobalCurrencyRateDetail> list = globalCurrencyRateDetailService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new GlobalCurrencyRateDetail
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		GlobalCurrencyRateDetail globalCurrencyRateDetail = new GlobalCurrencyRateDetail();	
		populateModel( model, globalCurrencyRateDetail, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing GlobalCurrencyRateDetail
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		GlobalCurrencyRateDetail globalCurrencyRateDetail = globalCurrencyRateDetailService.findById(id);
		populateModel( model, globalCurrencyRateDetail, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param globalCurrencyRateDetail  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid GlobalCurrencyRateDetail globalCurrencyRateDetail, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				GlobalCurrencyRateDetail globalCurrencyRateDetailCreated = globalCurrencyRateDetailService.create(globalCurrencyRateDetail); 
				model.addAttribute(MAIN_ENTITY_NAME, globalCurrencyRateDetailCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, globalCurrencyRateDetail.getId() );
			} else {
				populateModel( model, globalCurrencyRateDetail, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "globalCurrencyRateDetail.error.create", e);
			populateModel( model, globalCurrencyRateDetail, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param globalCurrencyRateDetail  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid GlobalCurrencyRateDetail globalCurrencyRateDetail, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				GlobalCurrencyRateDetail globalCurrencyRateDetailSaved = globalCurrencyRateDetailService.update(globalCurrencyRateDetail);
				model.addAttribute(MAIN_ENTITY_NAME, globalCurrencyRateDetailSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, globalCurrencyRateDetail.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, globalCurrencyRateDetail, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "globalCurrencyRateDetail.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, globalCurrencyRateDetail, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			globalCurrencyRateDetailService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "globalCurrencyRateDetail.error.delete", e);
		}
		return redirectToList();
	}

}
