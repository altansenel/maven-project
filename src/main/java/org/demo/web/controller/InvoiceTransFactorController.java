/*
 * Created on 24 Þub 2016 ( Time 15:56:49 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.demo.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import org.demo.web.common.AbstractController;
import org.demo.web.common.FormMode;
import org.demo.web.common.Message;
import org.demo.web.common.MessageType;

//--- Entities
import org.demo.bean.InvoiceTransFactor;
import org.demo.bean.StockCostFactor;
import org.demo.bean.InvoiceTrans;

//--- Services 
import org.demo.business.service.InvoiceTransFactorService;
import org.demo.business.service.StockCostFactorService;
import org.demo.business.service.InvoiceTransService;

//--- List Items 
import org.demo.web.listitem.StockCostFactorListItem;
import org.demo.web.listitem.InvoiceTransListItem;

/**
 * Spring MVC controller for 'InvoiceTransFactor' management.
 */
@Controller
@RequestMapping("/invoiceTransFactor")
public class InvoiceTransFactorController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "invoiceTransFactor";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "invoiceTransFactor/form";
	private static final String JSP_LIST   = "invoiceTransFactor/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/invoiceTransFactor/create";
	private static final String SAVE_ACTION_UPDATE   = "/invoiceTransFactor/update";

	//--- Main entity service
	@Resource
    private InvoiceTransFactorService invoiceTransFactorService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private StockCostFactorService stockCostFactorService; // Injected by Spring
	@Resource
    private InvoiceTransService invoiceTransService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public InvoiceTransFactorController() {
		super(InvoiceTransFactorController.class, MAIN_ENTITY_NAME );
		log("InvoiceTransFactorController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "StockCostFactor"
	 * @param model
	 */
	private void populateListOfStockCostFactorItems(Model model) {
		List<StockCostFactor> list = stockCostFactorService.findAll();
		List<StockCostFactorListItem> items = new LinkedList<StockCostFactorListItem>();
		for ( StockCostFactor stockCostFactor : list ) {
			items.add(new StockCostFactorListItem( stockCostFactor ) );
		}
		model.addAttribute("listOfStockCostFactorItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "InvoiceTrans"
	 * @param model
	 */
	private void populateListOfInvoiceTransItems(Model model) {
		List<InvoiceTrans> list = invoiceTransService.findAll();
		List<InvoiceTransListItem> items = new LinkedList<InvoiceTransListItem>();
		for ( InvoiceTrans invoiceTrans : list ) {
			items.add(new InvoiceTransListItem( invoiceTrans ) );
		}
		model.addAttribute("listOfInvoiceTransItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param invoiceTransFactor
	 */
	private void populateModel(Model model, InvoiceTransFactor invoiceTransFactor, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, invoiceTransFactor);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfStockCostFactorItems(model);
			populateListOfInvoiceTransItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfInvoiceTransItems(model);
			populateListOfStockCostFactorItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of InvoiceTransFactor found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<InvoiceTransFactor> list = invoiceTransFactorService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new InvoiceTransFactor
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		InvoiceTransFactor invoiceTransFactor = new InvoiceTransFactor();	
		populateModel( model, invoiceTransFactor, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing InvoiceTransFactor
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		InvoiceTransFactor invoiceTransFactor = invoiceTransFactorService.findById(id);
		populateModel( model, invoiceTransFactor, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param invoiceTransFactor  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid InvoiceTransFactor invoiceTransFactor, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				InvoiceTransFactor invoiceTransFactorCreated = invoiceTransFactorService.create(invoiceTransFactor); 
				model.addAttribute(MAIN_ENTITY_NAME, invoiceTransFactorCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, invoiceTransFactor.getId() );
			} else {
				populateModel( model, invoiceTransFactor, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "invoiceTransFactor.error.create", e);
			populateModel( model, invoiceTransFactor, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param invoiceTransFactor  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid InvoiceTransFactor invoiceTransFactor, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				InvoiceTransFactor invoiceTransFactorSaved = invoiceTransFactorService.update(invoiceTransFactor);
				model.addAttribute(MAIN_ENTITY_NAME, invoiceTransFactorSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, invoiceTransFactor.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, invoiceTransFactor, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "invoiceTransFactor.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, invoiceTransFactor, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			invoiceTransFactorService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "invoiceTransFactor.error.delete", e);
		}
		return redirectToList();
	}

}
