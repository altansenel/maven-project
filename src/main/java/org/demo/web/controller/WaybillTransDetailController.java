/*
 * Created on 24 Þub 2016 ( Time 15:56:52 )
 * Generated by Telosys Tools Generator ( version 2.1.1 )
 */
package org.demo.web.controller;

import java.util.LinkedList;
import java.util.List;

import javax.annotation.Resource;
import javax.servlet.http.HttpServletRequest;
import javax.validation.Valid;

import org.springframework.stereotype.Controller;
import org.springframework.ui.Model;
import org.springframework.validation.BindingResult;
import org.springframework.web.bind.annotation.PathVariable;
import org.springframework.web.bind.annotation.RequestMapping;
import org.springframework.web.servlet.mvc.support.RedirectAttributes;

//--- Common classes
import org.demo.web.common.AbstractController;
import org.demo.web.common.FormMode;
import org.demo.web.common.Message;
import org.demo.web.common.MessageType;

//--- Entities
import org.demo.bean.WaybillTransDetail;
import org.demo.bean.WaybillTransStatus;
import org.demo.bean.SaleSeller;
import org.demo.bean.WaybillTrans;
import org.demo.bean.WaybillTransSource;
import org.demo.bean.Stock;
import org.demo.bean.GlobalTransPoint;
import org.demo.bean.StockDepot;
import org.demo.bean.GlobalPrivateCode;
import org.demo.bean.Contact;

//--- Services 
import org.demo.business.service.WaybillTransDetailService;
import org.demo.business.service.WaybillTransStatusService;
import org.demo.business.service.SaleSellerService;
import org.demo.business.service.WaybillTransService;
import org.demo.business.service.WaybillTransSourceService;
import org.demo.business.service.StockService;
import org.demo.business.service.GlobalTransPointService;
import org.demo.business.service.StockDepotService;
import org.demo.business.service.GlobalPrivateCodeService;
import org.demo.business.service.ContactService;

//--- List Items 
import org.demo.web.listitem.WaybillTransStatusListItem;
import org.demo.web.listitem.SaleSellerListItem;
import org.demo.web.listitem.WaybillTransListItem;
import org.demo.web.listitem.WaybillTransSourceListItem;
import org.demo.web.listitem.StockListItem;
import org.demo.web.listitem.GlobalTransPointListItem;
import org.demo.web.listitem.StockDepotListItem;
import org.demo.web.listitem.GlobalPrivateCodeListItem;
import org.demo.web.listitem.ContactListItem;

/**
 * Spring MVC controller for 'WaybillTransDetail' management.
 */
@Controller
@RequestMapping("/waybillTransDetail")
public class WaybillTransDetailController extends AbstractController {

	//--- Variables names ( to be used in JSP with Expression Language )
	private static final String MAIN_ENTITY_NAME = "waybillTransDetail";
	private static final String MAIN_LIST_NAME   = "list";

	//--- JSP pages names ( View name in the MVC model )
	private static final String JSP_FORM   = "waybillTransDetail/form";
	private static final String JSP_LIST   = "waybillTransDetail/list";

	//--- SAVE ACTION ( in the HTML form )
	private static final String SAVE_ACTION_CREATE   = "/waybillTransDetail/create";
	private static final String SAVE_ACTION_UPDATE   = "/waybillTransDetail/update";

	//--- Main entity service
	@Resource
    private WaybillTransDetailService waybillTransDetailService; // Injected by Spring
	//--- Other service(s)
	@Resource
    private WaybillTransStatusService waybillTransStatusService; // Injected by Spring
	@Resource
    private SaleSellerService saleSellerService; // Injected by Spring
	@Resource
    private WaybillTransService waybillTransService; // Injected by Spring
	@Resource
    private WaybillTransSourceService waybillTransSourceService; // Injected by Spring
	@Resource
    private StockService stockService; // Injected by Spring
	@Resource
    private GlobalTransPointService globalTransPointService; // Injected by Spring
	@Resource
    private StockDepotService stockDepotService; // Injected by Spring
	@Resource
    private GlobalPrivateCodeService globalPrivateCodeService; // Injected by Spring
	@Resource
    private ContactService contactService; // Injected by Spring

	//--------------------------------------------------------------------------------------
	/**
	 * Default constructor
	 */
	public WaybillTransDetailController() {
		super(WaybillTransDetailController.class, MAIN_ENTITY_NAME );
		log("WaybillTransDetailController created.");
	}

	//--------------------------------------------------------------------------------------
	// Spring MVC model management
	//--------------------------------------------------------------------------------------
	/**
	 * Populates the combo-box "items" for the referenced entity "WaybillTransStatus"
	 * @param model
	 */
	private void populateListOfWaybillTransStatusItems(Model model) {
		List<WaybillTransStatus> list = waybillTransStatusService.findAll();
		List<WaybillTransStatusListItem> items = new LinkedList<WaybillTransStatusListItem>();
		for ( WaybillTransStatus waybillTransStatus : list ) {
			items.add(new WaybillTransStatusListItem( waybillTransStatus ) );
		}
		model.addAttribute("listOfWaybillTransStatusItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "SaleSeller"
	 * @param model
	 */
	private void populateListOfSaleSellerItems(Model model) {
		List<SaleSeller> list = saleSellerService.findAll();
		List<SaleSellerListItem> items = new LinkedList<SaleSellerListItem>();
		for ( SaleSeller saleSeller : list ) {
			items.add(new SaleSellerListItem( saleSeller ) );
		}
		model.addAttribute("listOfSaleSellerItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "WaybillTrans"
	 * @param model
	 */
	private void populateListOfWaybillTransItems(Model model) {
		List<WaybillTrans> list = waybillTransService.findAll();
		List<WaybillTransListItem> items = new LinkedList<WaybillTransListItem>();
		for ( WaybillTrans waybillTrans : list ) {
			items.add(new WaybillTransListItem( waybillTrans ) );
		}
		model.addAttribute("listOfWaybillTransItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "WaybillTransSource"
	 * @param model
	 */
	private void populateListOfWaybillTransSourceItems(Model model) {
		List<WaybillTransSource> list = waybillTransSourceService.findAll();
		List<WaybillTransSourceListItem> items = new LinkedList<WaybillTransSourceListItem>();
		for ( WaybillTransSource waybillTransSource : list ) {
			items.add(new WaybillTransSourceListItem( waybillTransSource ) );
		}
		model.addAttribute("listOfWaybillTransSourceItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Stock"
	 * @param model
	 */
	private void populateListOfStockItems(Model model) {
		List<Stock> list = stockService.findAll();
		List<StockListItem> items = new LinkedList<StockListItem>();
		for ( Stock stock : list ) {
			items.add(new StockListItem( stock ) );
		}
		model.addAttribute("listOfStockItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "GlobalTransPoint"
	 * @param model
	 */
	private void populateListOfGlobalTransPointItems(Model model) {
		List<GlobalTransPoint> list = globalTransPointService.findAll();
		List<GlobalTransPointListItem> items = new LinkedList<GlobalTransPointListItem>();
		for ( GlobalTransPoint globalTransPoint : list ) {
			items.add(new GlobalTransPointListItem( globalTransPoint ) );
		}
		model.addAttribute("listOfGlobalTransPointItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "StockDepot"
	 * @param model
	 */
	private void populateListOfStockDepotItems(Model model) {
		List<StockDepot> list = stockDepotService.findAll();
		List<StockDepotListItem> items = new LinkedList<StockDepotListItem>();
		for ( StockDepot stockDepot : list ) {
			items.add(new StockDepotListItem( stockDepot ) );
		}
		model.addAttribute("listOfStockDepotItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "GlobalPrivateCode"
	 * @param model
	 */
	private void populateListOfGlobalPrivateCodeItems(Model model) {
		List<GlobalPrivateCode> list = globalPrivateCodeService.findAll();
		List<GlobalPrivateCodeListItem> items = new LinkedList<GlobalPrivateCodeListItem>();
		for ( GlobalPrivateCode globalPrivateCode : list ) {
			items.add(new GlobalPrivateCodeListItem( globalPrivateCode ) );
		}
		model.addAttribute("listOfGlobalPrivateCodeItems", items ) ;
	}

	/**
	 * Populates the combo-box "items" for the referenced entity "Contact"
	 * @param model
	 */
	private void populateListOfContactItems(Model model) {
		List<Contact> list = contactService.findAll();
		List<ContactListItem> items = new LinkedList<ContactListItem>();
		for ( Contact contact : list ) {
			items.add(new ContactListItem( contact ) );
		}
		model.addAttribute("listOfContactItems", items ) ;
	}


	/**
	 * Populates the Spring MVC model with the given entity and eventually other useful data
	 * @param model
	 * @param waybillTransDetail
	 */
	private void populateModel(Model model, WaybillTransDetail waybillTransDetail, FormMode formMode) {
		//--- Main entity
		model.addAttribute(MAIN_ENTITY_NAME, waybillTransDetail);
		if ( formMode == FormMode.CREATE ) {
			model.addAttribute(MODE, MODE_CREATE); // The form is in "create" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_CREATE); 			
			//--- Other data useful in this screen in "create" mode (all fields)
			populateListOfWaybillTransStatusItems(model);
			populateListOfSaleSellerItems(model);
			populateListOfWaybillTransItems(model);
			populateListOfWaybillTransSourceItems(model);
			populateListOfStockItems(model);
			populateListOfGlobalTransPointItems(model);
			populateListOfStockDepotItems(model);
			populateListOfGlobalPrivateCodeItems(model);
			populateListOfContactItems(model);
		}
		else if ( formMode == FormMode.UPDATE ) {
			model.addAttribute(MODE, MODE_UPDATE); // The form is in "update" mode
			model.addAttribute(SAVE_ACTION, SAVE_ACTION_UPDATE); 			
			//--- Other data useful in this screen in "update" mode (only non-pk fields)
			populateListOfStockItems(model);
			populateListOfWaybillTransItems(model);
			populateListOfStockDepotItems(model);
			populateListOfContactItems(model);
			populateListOfSaleSellerItems(model);
			populateListOfWaybillTransSourceItems(model);
			populateListOfGlobalTransPointItems(model);
			populateListOfGlobalPrivateCodeItems(model);
			populateListOfWaybillTransStatusItems(model);
		}
	}

	//--------------------------------------------------------------------------------------
	// Request mapping
	//--------------------------------------------------------------------------------------
	/**
	 * Shows a list with all the occurrences of WaybillTransDetail found in the database
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping()
	public String list(Model model) {
		log("Action 'list'");
		List<WaybillTransDetail> list = waybillTransDetailService.findAll();
		model.addAttribute(MAIN_LIST_NAME, list);		
		return JSP_LIST;
	}

	/**
	 * Shows a form page in order to create a new WaybillTransDetail
	 * @param model Spring MVC model
	 * @return
	 */
	@RequestMapping("/form")
	public String formForCreate(Model model) {
		log("Action 'formForCreate'");
		//--- Populates the model with a new instance
		WaybillTransDetail waybillTransDetail = new WaybillTransDetail();	
		populateModel( model, waybillTransDetail, FormMode.CREATE);
		return JSP_FORM;
	}

	/**
	 * Shows a form page in order to update an existing WaybillTransDetail
	 * @param model Spring MVC model
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/form/{id}")
	public String formForUpdate(Model model, @PathVariable("id") Integer id ) {
		log("Action 'formForUpdate'");
		//--- Search the entity by its primary key and stores it in the model 
		WaybillTransDetail waybillTransDetail = waybillTransDetailService.findById(id);
		populateModel( model, waybillTransDetail, FormMode.UPDATE);		
		return JSP_FORM;
	}

	/**
	 * 'CREATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param waybillTransDetail  entity to be created
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/create" ) // GET or POST
	public String create(@Valid WaybillTransDetail waybillTransDetail, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'create'");
		try {
			if (!bindingResult.hasErrors()) {
				WaybillTransDetail waybillTransDetailCreated = waybillTransDetailService.create(waybillTransDetail); 
				model.addAttribute(MAIN_ENTITY_NAME, waybillTransDetailCreated);

				//---
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				return redirectToForm(httpServletRequest, waybillTransDetail.getId() );
			} else {
				populateModel( model, waybillTransDetail, FormMode.CREATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			log("Action 'create' : Exception - " + e.getMessage() );
			messageHelper.addException(model, "waybillTransDetail.error.create", e);
			populateModel( model, waybillTransDetail, FormMode.CREATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'UPDATE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param waybillTransDetail  entity to be updated
	 * @param bindingResult Spring MVC binding result
	 * @param model Spring MVC model
	 * @param redirectAttributes Spring MVC redirect attributes
	 * @param httpServletRequest
	 * @return
	 */
	@RequestMapping(value = "/update" ) // GET or POST
	public String update(@Valid WaybillTransDetail waybillTransDetail, BindingResult bindingResult, Model model, RedirectAttributes redirectAttributes, HttpServletRequest httpServletRequest) {
		log("Action 'update'");
		try {
			if (!bindingResult.hasErrors()) {
				//--- Perform database operations
				WaybillTransDetail waybillTransDetailSaved = waybillTransDetailService.update(waybillTransDetail);
				model.addAttribute(MAIN_ENTITY_NAME, waybillTransDetailSaved);
				//--- Set the result message
				messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"save.ok"));
				log("Action 'update' : update done - redirect");
				return redirectToForm(httpServletRequest, waybillTransDetail.getId());
			} else {
				log("Action 'update' : binding errors");
				populateModel( model, waybillTransDetail, FormMode.UPDATE);
				return JSP_FORM;
			}
		} catch(Exception e) {
			messageHelper.addException(model, "waybillTransDetail.error.update", e);
			log("Action 'update' : Exception - " + e.getMessage() );
			populateModel( model, waybillTransDetail, FormMode.UPDATE);
			return JSP_FORM;
		}
	}

	/**
	 * 'DELETE' action processing. <br>
	 * This action is based on the 'Post/Redirect/Get (PRG)' pattern, so it ends by 'http redirect'<br>
	 * @param redirectAttributes
	 * @param id  primary key element
	 * @return
	 */
	@RequestMapping(value = "/delete/{id}") // GET or POST
	public String delete(RedirectAttributes redirectAttributes, @PathVariable("id") Integer id) {
		log("Action 'delete'" );
		try {
			waybillTransDetailService.delete( id );
			//--- Set the result message
			messageHelper.addMessage(redirectAttributes, new Message(MessageType.SUCCESS,"delete.ok"));	
		} catch(Exception e) {
			messageHelper.addException(redirectAttributes, "waybillTransDetail.error.delete", e);
		}
		return redirectToList();
	}

}
